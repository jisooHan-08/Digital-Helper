import 'package:flutter/material.dart';

class TypingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('타자 연습')),
      body: Center(child: Text('여기에 타자 연습 기능이 들어갈 거예요')),
    );
  }
}
